1. Update timer to consume formattedTime instead of creating minutes and seconds separately

2. isRunning is redundant, use status

2. Create a TODO and reminder ruleset for Claude

3. On mobile screens, move the timer out of the header and into the sidebar. Sidebar should not be a modal