Run `git diff HEAD` to get all uncommitted changes, then analyze the diff for:
- Redundant or duplicated logic
- Unnecessary intermediate variables
- Excessive code that could be simplified
- Anything that contradicts the project's best practices in CLAUDE.md

Report each finding clearly — what it is, where it is, and why it's worth changing. Then ask the user whether to proceed with making the cleanup edits.
